#include<stdio.h>
int main()
{
	int a,b;
	scanf("%d%d",&a, &b);
	int add = a+b;
	int sub = a-b;
	int multiply = a*b;
	int divide = a/b;
	printf("add,sub,multiply,divide");
	return 0;
}

	#include <stdio.h>

	int main()
	{
   	 int a, b;
    	 printf("Enter two numbers: ");
   	 scanf("%d %d", &a, &b);
    
  	  int add = a + b;
  	  int sub = a - b;
  	  int multiply = a * b;
  	  int divide = a / b;
    	   printf("Addition: %d\n", add);
     	   printf("Subtraction: %d\n", sub);
     	   printf("Multiplication: %d\n", multiply);
      	   printf("Division: %d\n", divide);
    } 
    return 0;
}

