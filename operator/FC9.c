#include<stdio.h>

int main()

{ 
	char ch;
	print("enter any character");
	scanf("%c",&ch);
	if(ch >='a' && ch<= 'z')
	{ 
	printf("'%c'character in lowercase.",ch);
	}
	else if (ch<='A'&& ch<='Z')
	{
	printf("'%c' character in uppercase.",ch);
	"ch);
	}
	else
	{
	printf("'%c'is not alphabet."ch)
	}
	return 0;
	}
